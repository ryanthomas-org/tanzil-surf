
     // force mobile mode:

     opt.displayMode = 'mobile';
     update("curr.tab","quran");

    var setvar = document.getElementById("side-content");

    if(setvar) {
         window.location.reload();
     }

